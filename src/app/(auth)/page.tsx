// Cette route est un alias pour la racine "/" : le vrai code est dans src/app/page.tsx.
// Next.js détecte deux fichiers page mais on partage le même default export pour éviter le conflit.
export { default } from '../page';
